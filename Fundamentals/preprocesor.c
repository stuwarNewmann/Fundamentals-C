#include <stdio.h>

//Varible Inicialitation
#define float PIZZACOST 10.5;
const char NEWLINE = '\n';

int main()
{

    float costoPizzas;
    costoPizzas = PIZZACOST;

    printf("Cost of Pizza is: %f", costoPizzas);
    printf("%c", NEWLINE);
    return 0;
}